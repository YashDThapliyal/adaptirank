# Data

The primary future retrieval/ranking backbone is Amazon Shopping Queries (ESCI). M1 will keep
the Task 1 US small variant distinct from the explicit large variant. Open Bandit, Criteo, and
KuaiRand/KuaiRec remain `NOT_RUN` external tracks and are never merged into a fabricated log.

